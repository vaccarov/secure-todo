import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  button: {
    backgroundColor: '#004a98ff',
    borderRadius: 10,
    padding: 10,
    alignItems: 'center',
    justifyContent: 'center'
  },
  disabledButton: {
    backgroundColor: '#cccccc'
  }
});
